#WH 2nd passsword genorator

#
#
#Functions for the different password requirements
    # set the character list
    #ask the user if they want to have uppercase letters
    #if yes
        #add the uppercase letters to the character list
    #if no
    #ask the user if they want to haav lowercase letters
    #if yes
        #add the uppercase letters to the character list
    #if no
    #ask the user if they want to have numbers
    #if yes
        #add the uppercase letters to the character list
    #if no
    #ask the user if they want to have special characters
    #if yes 
        #add the uppercase letters to the character list
    #if no
#main funtion for the menu


match